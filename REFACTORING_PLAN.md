# リファクタリング計画

## 目的

現在の動作と公開 API を維持しながら、巨大クラスに集中している責務を分離し、端末プロトコル、セッション管理、描画、UI の各領域を独立してテスト・変更できる構造にする。

## 基本方針

- 機能追加とリファクタリングを同じコミットに混在させない。
- 各段階の開始前に、対象となる現行動作を特性テストで固定する。
- 各段階を独立したコミットとして完結させる。
- 各段階は、必要に応じて「テスト固定、型抽出、呼び出し置換、不要コード削除」のレビュー可能なコミットへ分割する。
- 実装後に独立したレビューを行い、指摘を反映してからコミットする。
- 各コミット前に `dotnet test Terminal.sln` を実行し、全テスト成功を必須とする。
- 公開 API の変更が必要な場合は、互換性への影響を明記して別途判断する。
- 行数削減だけを完了条件にせず、状態の所有権とテスト可能な境界が改善したことを確認する。
- ビルド、WPF テスト、ConPTY 検証は Windows 上で行う。

## ベースライン

- 構成: `Terminal.Core` / `Terminal.Controls` / `Terminal.App` / `Terminal.Tests`
- テスト: 533 件成功、失敗・スキップなし（2026-07-04）
- 作業ツリー: 計画開始時点でクリーン
- 主な責務集中箇所:
  - `AnsiTerminalBuffer.cs`: 約 4,600 行
  - `TerminalTabView` の partial 群: 約 4,500 行
  - `TerminalSurfaceControl.cs`: 約 2,400 行
  - `ConPtySession.cs`: 約 1,000 行
  - `MainWindow.xaml.cs`: 約 900 行

## 段階 1: 現行動作を特性テストで固定する

### 対象

- VT パーサーの CSI / OSC / DCS 入力と状態遷移
- リサイズ、リフロー、スクロールバック、alternate screen
- セッションの開始、終了、出力 drain、破棄
- IME、キー入力、選択、検索の純粋ロジック

### 作業

1. 既存テストのカバレッジ分布を確認する。
2. 既存テストと重複させず、分割入力、異常シーケンスからの回復、イベント順序、wide/combining character を含むリフローなど、後続の抽出で壊れやすい境界を優先して特性テストを追加する。
3. 実装詳細ではなく、入力・出力・イベント・公開状態を検証する。

### 完了条件

- 後続段階で移動する主要な振る舞いに回帰テストがある。
- テストが現在の実装構造や private メソッドに過度に依存していない。
- 全テストが成功する。

## 段階 2: `AnsiTerminalBuffer` を責務分離する

### 抽出候補

- VT パーサー状態機械
- CSI / OSC / DCS ディスパッチ
- 画面バッファとスクロールバック
- リサイズとリフロー
- SGR、パレット、色処理
- 描画・文書スナップショット生成

### 進め方

1. 依存の少ない純粋ロジックから抽出する。
2. 共有可変状態を無制限に公開せず、明示的な操作 API を設ける。
3. 抽出ごとに特性テストを維持し、小さいコミットに分ける。

### 完了条件

- `AnsiTerminalBuffer` は端末状態の統括と処理の委譲に集中している。
- プロトコル解析、リフロー、色処理を個別にテストできる。
- 入力シーケンス、イベント、スナップショットの互換性が維持される。
- `Process` 呼び出しをまたぐパーサー状態と、分割入力の結果が維持される。

## 段階 3: `TerminalTabView` を UI とサービスへ分離する

### 抽出候補

- セッションライフサイクル
- 出力キューと描画スケジューリング
- IME とキーボード入力の調停
- クリップボードとマウス操作
- 検索と履歴の状態管理
- プロファイルと起動要求の構築

### 進め方

1. partial ファイルの追加だけで終わらせず、UI 非依存クラスへ状態と判断を移す。
2. code-behind は WPF イベントの受け渡しと View 反映に限定する。
3. 非同期セッション処理ではキャンセル、終了、再起動の競合をテストする。

### 完了条件

- 主要な状態遷移を WPF ウィンドウなしでテストできる。
- UI イベントハンドラーがドメイン判断を直接持たない。
- 起動、停止、再起動、IME、検索の既存動作が維持される。

## 段階 4: `TerminalSurfaceControl` を責務分離する

### 抽出候補

- 選択と検索モデル
- テキスト位置、セル位置、座標の変換
- ハイパーリンク検出
- 行レイアウト生成
- 描画キャッシュと描画コマンド
- スクロール状態

### 完了条件

- 文字位置計算、検索、選択を STA/WPF コントロールなしでテストできる。
- 描画キャッシュの生成と破棄責任が明確である。
- `TextLine` などの `IDisposable` な描画資源がスナップショット交換時に確実に破棄される。
- Unicode、合字、wide character、スクロール位置の動作が維持される。

## 段階 5: `ConPtySession` の境界を分離する

### 抽出候補

- Win32 ハンドルの所有と解放
- pseudo console の生成とリサイズ
- プロセス起動と環境変数構築
- 非同期入出力ポンプ
- 終了監視と shutdown 手順

### 完了条件

- ハンドルの所有者と破棄順序がコード上で明確である。
- 多重 Dispose、起動失敗、出力 drain、強制終了を検証できる。
- read、process exit、Dispose の競合時にもイベントが重複せず、ハンドルを二重解放しない。
- ConPTY の実機 smoke test と全自動テストが成功する。

## 段階 6: App 層を薄くする

### 対象

- `MainWindow` のタブ管理、ウィンドウ設定、レイアウト判断
- `SettingsWindow` の設定編集と検証

### 完了条件

- Window code-behind はイベント転送と表示更新を中心とする。
- タブ操作と設定変換のロジックを単体テストできる。
- ウィンドウ配置、タブ配置、設定保存の動作が維持される。

## 段階 7: 公開境界、パッケージ構成、文書を整理する

### 作業

- `Terminal.Core` の WPF 依存を棚卸しし、必要なら型境界を整理する。
- `InternalsVisibleTo` の利用箇所を確認し、不要な公開範囲を削減する。
- Controls パッケージへの Core DLL 同梱とバージョニングを検証する。
- `dotnet pack` の生成物と、生成したパッケージを参照する最小 consumer のビルドを検証する。
- README のプロジェクト構成、ビルド、テスト手順を現状に合わせる。

### 完了条件

- プロジェクト間の依存方向が一方向で説明可能である。
- パッケージ生成物をクリーン環境相当で参照できる。
- 文書が実際のディレクトリ構成とコマンドに一致する。

## 各段階のレビュー観点

- 既存動作・公開 API・イベント順序を意図せず変更していないか。
- 新しい抽象が単なる転送層や循環依存を作っていないか。
- mutable state の所有者とスレッド境界が明確か。
- 非同期処理の例外、キャンセル、破棄が観測可能か。
- WPF Dispatcher とバックグラウンドスレッドの境界が保たれているか。
- テストが抽出後の内部構造ではなく外部の振る舞いを検証しているか。
- parser のチャンク境界、resize/reflow の cursor・wrapped flag・absolute index、各種 Dispose の所有権が維持されているか。
- public/internal API surface と `InternalsVisibleTo` の対象が意図せず拡大していないか。

## コミット運用

各作業単位で次を実施する。

1. 対象範囲と受け入れ条件を決める。
2. 実装担当が変更とテストを作成する。
3. レビュー担当が差分、設計、回帰リスクを確認する。
4. 指摘を反映する。
5. `git diff --check`、`dotnet build Terminal.sln`、`dotnet test Terminal.sln` を実行する。
6. 変更目的が一つになるようコミットする。

コミットメッセージは Conventional Commits を基本とし、テスト固定は `test:`、内部構造の変更は `refactor:`、文書のみの変更は `docs:` を使用する。

<Window x:Class="ConPtyTerminal.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Settings"
        Width="520"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#131313"
        Foreground="#F0F0F0">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1D1D1D" />
        <SolidColorBrush x:Key="InputBrush" Color="#0F0F0F" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2A2A2A" />
        <SolidColorBrush x:Key="AccentBrush" Color="#4BA3FF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#A6A6A6" />
        <SolidColorBrush x:Key="HoverBrush" Color="#1A1A1A" />
        <SolidColorBrush x:Key="SelectedBrush" Color="#1E3552" />
        <SolidColorBrush x:Key="InvalidBrush" Color="#D35F5F" />

        <DataTemplate x:Key="TerminalProfileItemTemplate">
            <TextBlock Text="{Binding DisplayName}" />
        </DataTemplate>

        <DataTemplate x:Key="FontFamilyItemTemplate">
            <TextBlock Text="{Binding}" FontFamily="{Binding}" />
        </DataTemplate>

        <DataTemplate x:Key="TabStripPlacementItemTemplate">
            <TextBlock Text="{Binding DisplayName}" />
        </DataTemplate>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="primitives:ToggleButton">
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="primitives:ToggleButton">
                        <Border x:Name="ToggleBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ContentPresenter Margin="10,0,34,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left" />
                                <Path x:Name="ArrowPath"
                                      Width="10"
                                      Height="6"
                                      Margin="0,0,12,0"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 5 5 L 10 0"
                                      Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="1.5"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleBorder" Property="Background" Value="{StaticResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ToggleBorder" Property="Background" Value="#202020" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="ArrowPath" Property="Stroke" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ToggleBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Margin="4,2"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SelectedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                <Setter TargetName="ItemBorder" Property="Opacity" Value="0.65" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource InputComboBoxItemStyle}" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <primitives:ToggleButton x:Name="ComboToggleButton"
                                                     Background="{TemplateBinding Background}"
                                                     BorderBrush="{TemplateBinding BorderBrush}"
                                                     BorderThickness="{TemplateBinding BorderThickness}"
                                                     Foreground="{TemplateBinding Foreground}"
                                                     Focusable="False"
                                                     ClickMode="Press"
                                                     IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                     Style="{StaticResource ComboBoxToggleButtonStyle}">
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                            </primitives:ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,4,0,0"
                                        Background="{StaticResource PanelBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="10">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboToggleButton" Property="Opacity" Value="0.65" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border Margin="14"
            Padding="16"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Profile" />
            <ComboBox x:Name="ProfileComboBox"
                      Grid.Row="0"
                      Grid.Column="1"
                      Style="{StaticResource InputComboBoxStyle}"
                      ItemTemplate="{StaticResource TerminalProfileItemTemplate}"
                      TextSearch.TextPath="DisplayName"
                      SelectionChanged="ProfileComboBox_SelectionChanged" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Command" />
            <TextBox x:Name="CommandTextBox"
                     Grid.Row="1"
                     Grid.Column="1"
                     Margin="0,10,0,0"
                     Style="{StaticResource InputTextBoxStyle}"
                     TextChanged="CommandTextBox_TextChanged" />

            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Start in" />
            <TextBox x:Name="WorkingDirectoryTextBox"
                     Grid.Row="2"
                     Grid.Column="1"
                     Margin="0,10,0,0"
                     Style="{StaticResource InputTextBoxStyle}"
                     TextChanged="WorkingDirectoryTextBox_TextChanged"
                     LostKeyboardFocus="WorkingDirectoryTextBox_LostKeyboardFocus" />

            <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Font" />
            <ComboBox x:Name="FontFamilyComboBox"
                      Grid.Row="3"
                      Grid.Column="1"
                      Margin="0,10,0,0"
                      Style="{StaticResource InputComboBoxStyle}"
                      ItemTemplate="{StaticResource FontFamilyItemTemplate}"
                      SelectionChanged="FontFamilyComboBox_SelectionChanged" />

            <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Font size" />
            <TextBox x:Name="FontSizeTextBox"
                     Grid.Row="4"
                     Grid.Column="1"
                     Margin="0,10,0,0"
                     Width="84"
                     HorizontalAlignment="Left"
                     Style="{StaticResource InputTextBoxStyle}"
                     TextChanged="FontSizeTextBox_TextChanged"
                     LostKeyboardFocus="FontSizeTextBox_LostKeyboardFocus" />

            <TextBlock Grid.Row="5"
                       Grid.Column="0"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}"
                       Text="Tabs" />
            <ComboBox x:Name="TabStripPlacementComboBox"
                      Grid.Row="5"
                      Grid.Column="1"
                      Margin="0,10,0,0"
                      Style="{StaticResource InputComboBoxStyle}"
                      ItemTemplate="{StaticResource TabStripPlacementItemTemplate}"
                      SelectionChanged="TabStripPlacementComboBox_SelectionChanged" />
        </Grid>
    </Border>
</Window>
